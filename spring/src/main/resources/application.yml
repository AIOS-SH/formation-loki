management:
  endpoints:
    enabled-by-default: true
    web:
      exposure.include: "*"
  endpoint:
    env:
      enabled: true
    health:
      enabled: true
      show-details: always
    info:
      enabled: true
    metrics:
      enabled: true
    bindings:
      enabled: true
    prometheus:
      enabled: true
